
setup that prior work suggests reliance on family specific motifs and coevolution - basically the argument is between if the model is creating a sequence identity or doing some kind of geometric logic or structural fold logic - this can be strong but its not clear at all what the binary actually means. This is kinda because the results aren’t here yet fully. 

Use contact prediction and attn circuits for this - because contact pred is structure proxy, has been explored, and it’s majorly based on attn patterns. 

Show that across 31 proteins that show the contact prediction jump discovered by Zhang et al., 2 sets of heads emerge that are reused across proteins - here 31 is low predictive power but we can scale to esm2-3b and that will have more proteins with jumps. We might discover later on that this isn’t even needed and you can just work with the anchor flank. 

First set are the interaction heads - these are basically contact readouts though they can be coupling heads too. This is because esm was never trained to do contact pred, so likely the model is using these heads to get the direct coupling pairs which are useful for masked token prediction. Though need to test that hypotheses to the extent it has impact. 

Second are anchor heads (name can be changed) these are vertical attn patterns - all queries look at the same key. Most of the attn mass is covered by top 3 keys. This is consistent across 2k proteins so very monosemantic / or at least consistent in what it does. 
(Show plot of universal pattern stuff)

Queries are highly self aligned within a proteins (obvious) but also high cosine sim across proteins - which implies it’s looking for the same thing / property. Creating a projection vector with the mean query of one protein works very well to reproduce the anchor keys in other proteins. 
(Show plot of query alignment and projection on other proteins )

No simple residue level properties are able to predict the anchor position 

No single motif in aa or 3di emerges. And sequence similarity is also not high. 

Finding proteins with high structural but low seq sim - anchors are similar and shared the alignment column! 

So what? 
The model is not doing family recognition nor looking at sequence level motifs, it’s doing something structural. Looking at structural properties. 

Is that enough? I don’t know 

Will have to show paths between anchor and interaction heads - doable but as the head is in the circuit, this isn't about the causal effect of anchor on 
